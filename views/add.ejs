<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Add Employee • Payroll</title>

  <link href="https://cdn.jsdelivr.net/npm/remixicon@3.5.0/fonts/remixicon.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="container">
    <header class="header">
      <h1><span class="logo"><i class="ri-user-add-line"></i></span> Add Employee</h1>
      <p>Enter basic details — salary will be used to calculate tax and net pay</p>
    </header>

    <div class="card">
      <% if (error) { %>
        <div class="error"><%= decodeURIComponent(error) %></div>
      <% } %>

      <form action="/add" method="POST" class="form-grid">
        <div class="form-row">
          <label><i class="ri-user-line"></i> Full name</label>
          <input name="name" type="text" placeholder="e.g. Jane Doe" required>
        </div>

        <div class="form-row">
          <label><i class="ri-building-line"></i> Department</label>
          <input name="department" type="text" placeholder="e.g. Finance">
        </div>

        <div class="form-row">
          <label><i class="ri-money-rupee-circle-line"></i> Basic Salary (₹)</label>
          <input name="basicSalary" type="number" placeholder="50000" min="0" step="0.01" required>
        </div>

        <div class="form-row full" style="display:flex;gap:10px;justify-content:flex-end">
          <a class="btn btn-outline" href="/">Cancel</a>
          <button class="btn btn-primary" type="submit"><i class="ri-check-line"></i> Create</button>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
